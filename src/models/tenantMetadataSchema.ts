import mongoose from "mongoose";

export const tenantMetadataSchema = new mongoose.Schema({
    tenantName: {
        type: String,
        required: true,
    },
    createdAt: {
        type: Date,
        default: Date.now,
    },
    updatedAt: {
        type: Date,
        default: Date.now,
    }
});
